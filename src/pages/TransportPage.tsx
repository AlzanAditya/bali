import { useMemo, useState } from 'react'; import { SEO } from '../components/SEO/SEO'; import { PageHero, SectionIntro } from '../components/Hero/Hero'; import { TransportCard } from '../components/Cards/Cards'; import { transport } from '../data/transport';
export function TransportPage(){const [filter,setFilter]=useState('all');const list=useMemo(()=>transport.filter(t=>filter==='all'||(filter==='Luxury'?['Toyota Alphard','Toyota Fortuner'].includes(t.title):filter==='MVP / Van'?['Toyota Avanza','Toyota Innova Reborn'].includes(t.title):filter==='Minibus'?t.title==='Toyota Hi-Ace':true)),[filter]);return <><SEO title="Private Transport & Airport Transfers in Bali - Bali Bagus Journey" description="Book private transport and airport transfers in Bali with Bali Bagus Journey." canonical="/transport.html"/><PageHero eyebrow="Transportation Service" title="Private Driver Service for Every Bali Trip" text="Choose from our fleet of comfortable vehicles and trusted local drivers."/><section id="transport-vehicle" className="px-4 py-14 md:px-8 lg:px-12 lg:py-20"><div className="mx-auto max-w-7xl"><SectionIntro title="Filter Transports by:"/><div className="mb-8 flex flex-wrap gap-2">{['all','Standard','MVP / Van','Minibus','Luxury'].map(x=><button key={x} onClick={()=>setFilter(x)} className={`gsap-transport-filter-tab rounded-full border border-border px-4 py-2 text-sm font-semibold ${filter===x?'bg-foreground text-white':'bg-white'}`}>{x==='all'?'All Transports':x}</button>)}</div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{list.map(t=><TransportCard key={t.slug} item={t}/>)}</div></div></section><section className="relative overflow-hidden bg-foreground px-4 py-20 text-white md:px-8 lg:px-12"><div className="relative z-10 mx-auto max-w-7xl"><h2 className="max-w-2xl text-3xl font-semibold lg:text-5xl">Ready to explore Bali with a driver you can trust?</h2><a href="/contact.html" className="mt-8 inline-flex rounded-full bg-primary px-6 py-3 font-semibold text-white">Plan Your Trip ↗</a></div></section></>}
