import { useState } from 'react';
export function Accordion({items}:{items:{question:string;answer:string}[]}){const [open,setOpen]=useState<number|null>(null);return <div className="grid gap-3">{items.map((item,i)=><div key={item.question} className="faq-item overflow-hidden rounded-2xl border border-border bg-card" aria-expanded={open===i}><button className="flex w-full items-center justify-between gap-5 p-5 text-left font-semibold" onClick={()=>setOpen(open===i?null:i)}><span>{item.question}</span><span className={`flex size-9 shrink-0 items-center justify-center rounded-full ${open===i?'bg-primary text-white':'bg-foreground text-background'}`}>{open===i?'−':'+'}</span></button><div className={`grid transition-[grid-template-rows] duration-500 ${open===i?'grid-rows-[1fr]':'grid-rows-[0fr]'}`}><div className="min-h-0 overflow-hidden"><p className="px-5 pb-5 leading-7 text-text-label-secondary">{item.answer}</p></div></div></div>)}</div>}
