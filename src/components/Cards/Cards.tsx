import { Link } from '@tanstack/react-router';
import type { Tour } from '../../data/tours';
import type { Blog } from '../../data/blogs';
import type { Transport } from '../../data/transport';

export function TourCard({tour}:{tour:Tour}){return <article className="gsap-tour-card tour-item group overflow-hidden rounded-3xl bg-card opacity-0 translate-y-16"><Link to={`/tours/${tour.slug}.html` as any} className="block"><div className="relative aspect-[1.25] overflow-hidden"><img src={tour.image} alt={tour.title} className="size-full object-cover transition-transform duration-700 group-hover:scale-105"/><div className="absolute inset-x-0 bottom-0 flex items-end justify-between bg-gradient-to-t from-black/75 to-transparent p-5 pt-16"><span className="text-xs font-semibold uppercase tracking-wide text-white/75">{tour.category}</span><span className="rounded-full bg-white px-3 py-1 text-sm font-semibold text-foreground">${tour.price}</span></div></div><div className="p-5"><h3 className="mb-2 line-clamp-2 text-lg font-semibold lg:text-xl">{tour.title}</h3><p className="line-clamp-3 text-sm leading-6 text-text-label-secondary">{tour.description}</p><span className="mt-5 inline-flex rounded-full bg-white px-4 py-2 text-sm font-semibold">View Tour ↗</span></div></Link></article>}
export function BlogCard({blog}:{blog:Blog}){return <article className="gsap-blog-card blog-item overflow-hidden rounded-3xl bg-card opacity-0 translate-y-16"><Link to={`/blogs/${blog.slug}.html` as any} className="group block"><div className="aspect-[1.5] overflow-hidden"><img src={blog.image} alt={blog.title} className="size-full object-cover transition-transform duration-700 group-hover:scale-105"/></div><div className="p-5"><div className="mb-2 text-xs font-semibold uppercase tracking-wide text-primary">{blog.category}</div><h3 className="text-xl font-semibold leading-snug">{blog.title}</h3><p className="mt-3 line-clamp-3 text-sm leading-6 text-text-label-secondary">{blog.excerpt}</p><span className="mt-5 inline-flex rounded-full bg-foreground px-4 py-2 text-sm font-semibold text-white">Read Article ↗</span></div></Link></article>}
export function TransportCard({item}:{item:Transport}){return <article className="gsap-transport-card transport-item overflow-hidden rounded-3xl bg-card opacity-0 translate-y-16"><Link to={`/transport/${item.slug}.html` as any} className="group block"><div className="aspect-[1.25] overflow-hidden"><img src={item.image} alt={item.title} className="size-full object-cover transition-transform duration-700 group-hover:scale-105"/></div><div className="p-5"><h3 className="text-lg font-semibold lg:text-xl">{item.title}</h3><div className="mt-3 flex gap-4 text-sm text-text-label-secondary"><span>{item.passengers} Pax</span><span>{item.bags} Bags</span></div><div className="mt-5 flex items-center justify-between"><span className="text-xl font-bold">${item.price}</span><span className="rounded-full bg-white px-4 py-2 text-sm font-semibold">View Detail ↗</span></div></div></Link></article>}
